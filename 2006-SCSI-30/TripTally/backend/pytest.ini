[pytest]
# Pytest configuration for TripTally backend tests

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Directories to search for tests
testpaths = tests

# Add app to Python path
pythonpath = .

# Environment variables for testing
env = 
    GOOGLE_MAPS_API_KEY=test_api_key_for_testing
    GOOGLE_SERVER_API_KEY=test_api_key_for_testing
    REQUEST_TIMEOUT=10
    CACHE_TTL_SECONDS=300

# Output options
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings

# Markers for categorizing tests
markers =
    unit: Unit tests (no database)
    integration: Integration tests (with database)
    slow: Slow-running tests
    
# Minimum coverage threshold (optional)
# Uncomment to enforce coverage requirements
# [coverage:run]
# source = app
# omit = 
#     */tests/*
#     */venv/*
#     */__pycache__/*

# [coverage:report]
# fail_under = 80
# show_missing = True
